apiVersion: v1
kind: Service
metadata:
  name: eks-demo-service
  labels:
    app: awesome-app
spec:
  type: NodePort
  selector:
    app: awesome-app
  ports:
    - port: 8080
      targetPort: 8080

